{
  // Configuration opencode pour symlinkrepair
  "run": "uvicorn app.main:app --reload --port 8000",
  "lint": "ruff check .",
  "typecheck": "",
  "test": "",
  "agents": {
    "stack": "FastAPI + Jinja2 + HTMX + Alpine.js + SQLite + Docker",
    "rules": "Consulter AGENTS.md pour les conventions du projet"
  }
}
